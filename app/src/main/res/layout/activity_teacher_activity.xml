<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".teacher_activity"
    android:id="@+id/teacher_drawer"
    tools:openDrawer="start">
    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        >

        <androidx.appcompat.widget.Toolbar
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/toolbar_teacher"
            app:title="Home"
            app:titleTextColor="@color/textcolor"
            android:background="@color/colorPrimary"
            />

        <FrameLayout
            android:id="@+id/teacher_framelayout"
            android:layout_width="match_parent"
            android:layout_height="674dp"
            android:layout_below="@+id/toolbar_teacher"
            />

    </RelativeLayout>
    <com.google.android.material.navigation.NavigationView
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:id="@+id/teacher_menu"
        android:layout_gravity="start"
        app:menu="@menu/activity_teacher_drawer"
        app:headerLayout="@layout/header_teacher"

        />


</androidx.drawerlayout.widget.DrawerLayout>